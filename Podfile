# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'XCUITestTemplate' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XCUITestTemplate

  target 'XCUITestTemplateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'XCUITestTemplateUITests' do
    inherit! :search_paths
     pod 'Cucumberish'
    # Pods for testing
  end

end
